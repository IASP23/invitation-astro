---
const { title, src } = Astro.props;
---

<img
  class="rounded-md p-2 contrast-125 hover:contrast-150 hover:shadow-2xl"
  src={src}
  alt={title}
/>

<style>
  img {
    clip-path: polygon(
      30% 0%,
      70% 0%,
      100% 30%,
      100% 70%,
      70% 100%,
      30% 100%,
      0% 70%,
      0% 30%
    );
  }
</style>
